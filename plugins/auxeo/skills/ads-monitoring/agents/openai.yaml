interface:
  display_name: "Ads · 监控与告警"
  short_description: "聚合余额、状态、政策、数据刷新和投递风险"
  default_prompt: "使用 $ads-monitoring 检查指定渠道今天必须处理的广告监控和数据运行风险，并保留证据与无法判断项。"

dependencies:
  tools:
    - type: "mcp"
      value: "ads_read"
      description: "Ads Read MCP server with governed monitoring evidence"
      transport: "stdio"

policy:
  allow_implicit_invocation: true
