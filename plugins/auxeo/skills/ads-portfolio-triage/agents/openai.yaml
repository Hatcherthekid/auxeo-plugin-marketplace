interface:
  display_name: "广告组合分诊"
  short_description: "从多 App、多渠道和大量广告对象中筛出值得继续分析的问题对象"
  default_prompt: "使用 $ads-portfolio-triage 解释已有 daily scan，并给出需要继续下钻的对象。"

dependencies:
  tools:
    - type: "mcp"
      value: "auxeo_ads_read"
      description: "Ads Read MCP server"
      transport: "stdio"

policy:
  allow_implicit_invocation: true
